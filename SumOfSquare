class Solution {
    public int sumOfSquares(int[] nums) 
    {
        int i,n,sum=0;
        n = nums.length;
        for(i=0;i<n;i++)
        {
            if(n% (i+1) ==0)
            {
                sum = sum + (nums[i]*nums[i]);
            }
        }
        return sum;
    }
}
